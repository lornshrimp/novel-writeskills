---
name: 异能志怪-生成章节控制卡
description: 'Use when: 写前控制卡、章节控制卡、章节作战卡、场景单元拆分、本章任务、本章回报、冷线回归、章名候选、章末钩子、章节施工卡。常见说法：先给我这章控制卡、这一章到底该推进什么、帮我把这章拆成几个场景、本章写前作战卡、这章的章名候选。'
argument-hint: '要给哪一章或哪组章纲生成控制卡？默认同时检查卷纲、冷线、回报、章末钩子与章名候选。'
---

# 生成章节控制卡

这是题材包装层与兼容入口。命中本技能时，**必须同时加载并使用**当前题材 Skill 与 `通用-生成章节控制卡`；当前题材 Skill 负责题材口径与边界，`通用-生成章节控制卡` 负责共性骨架与通用流程。

## 本层职责

- 保留旧入口名。
- 把写前控制任务路由到 `通用-生成章节控制卡`。
- 不再在本文件重复维护已迁移到通用层的控制卡规则、模板与检查表。

## 常见触发词 / 用户说法速查

- “先给我这章控制卡”
- “这章到底要推进什么”
- “帮我拆成 3-5 个场景单元”
- “哪些冷线该在这章回来了”
- “这章的章名候选先给我”
- “这章中段回报是什么”
- “写前作战卡 / 施工卡”

## 什么时候调用通用 skill

当任务属于以下任一情形时，加载并遵循本 Skill：

- 在正式写章节前，需要把卷纲 / 章纲压缩成可执行的“本章控制卡”。
- 中段容易塌，需要先明确本章任务、回报、冷线回归和钩子。
- 用户希望先看章名候选、POV、节奏档位、场景单元，再决定是否动笔。

## 题材补充边界

- 若案件骨架来自 `异能志怪-设计事件案件引擎`，本包装层负责把该上游结果接入通用控制卡流程。
- 本包装层不额外追加题材专属控制卡规则。

## 强制要求

- 不得绕过 `通用-生成章节控制卡` 另写一套平行规则。
- 不得在本文件恢复已迁移到通用层的 references 调度内容。
- 如需修改共性控制卡规则，应直接修改通用 skill。
